#include <stdio.h>
#include <limits.h>

int main() {
	printf("Integer Max=%d, Min=%d\n", INT_MAX, INT_MIN);
	printf("Long Max=%li, Min=%li\n", LONG_MAX, LONG_MIN);
	printf("Long Max=%lu, Min=%lu\n", LONG_MAX, LONG_MIN);
}
